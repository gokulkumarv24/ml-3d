# Core dependencies for mesh processing
numpy>=1.21.0
matplotlib>=3.5.0
trimesh>=3.15.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Optional but recommended
jupyter>=1.0.0
seaborn>=0.11.0

# For better mesh visualization (optional)
# open3d>=0.13.0  # Uncomment if needed, but may have installation issues on some systems